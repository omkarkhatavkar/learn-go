module learning/modules

go 1.22

// A Go module is a collection of Go packages that are released together.
// This `go.mod` file defines the module's path and its dependencies.
// The `go mod tidy` command adds and removes required dependencies to this file.
// You can manage dependencies with `go get <package>@<version>`.

// To initialize a module, run `go mod init <module_path>`.
// To build and run this example, navigate to the `09_modules_and_packages` folder
// and run `go run main.go`.
